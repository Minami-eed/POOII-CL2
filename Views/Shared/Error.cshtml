@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<style>
    .error-container {
        max-width: 600px;
        margin: 0 auto;
        padding: 40px;
        text-align: center;
        background-color: #f8f9fa;
        border-radius: 5px;
    }

    .error-heading {
        font-size: 32px;
        font-weight: bold;
        color: #dc3545;
        margin-bottom: 20px;
    }

    .error-message {
        font-size: 24px;
        font-weight: bold;
        color: #dc3545;
        margin-bottom: 20px;
    }

    .error-details {
        font-size: 16px;
        color: #6c757d;
        margin-bottom: 20px;
    }

    .development-info {
        font-size: 16px;
        color: #6c757d;
        margin-bottom: 20px;
    }
</style>

<div class="error-container">
    <h1 class="error-heading">Error</h1>
    <h2 class="error-message">Se produjo un error al procesar su solicitud.</h2>

    @if (Model.ShowRequestId)
    {
        <p class="error-details">
            <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
        </p>
    }

    <h3>Modo de desarrollo</h3>
    <p class="development-info">
        Cambiar al entorno de <strong>Desarrollo</strong> mostrará información más detallada sobre el error que ocurrió.
    </p>
    <p class="development-info">
        <strong>El entorno de Desarrollo no debe habilitarse para aplicaciones en producción.</strong>
        Puede resultar en mostrar información sensible de las excepciones a los usuarios finales.
        Para depurar localmente, habilita el entorno de <strong>Desarrollo</strong> configurando la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> en <strong>Development</strong>
        y reiniciando la aplicación.
    </p>
</div>
